# Enable or disable headless mode (True = run in background without opening browser)
HEADLESS = True

# Enable or disable filtering by price
FILTER_BY_PRICE = False

# Set the price threshold for filtering (used only if FILTER_BY_PRICE is True)
PRICE_THRESHOLD = 1000.0

# Delay in seconds to wait for page to load
SCRAPE_DELAY = 5

# Output CSV file path
CSV_FILE_PATH = "data/crypto_prices.csv"
